# ADR-021: FMC 灵活存储控制器

## 状态

Proposed

## Phase

P2

## 目标

> GitHub Issue: #26
> 验证级别: MEASURED

实现 STM32N6 FMC（灵活存储控制器）驱动，
支持连接 SRAM、NOR Flash、NAND Flash 等并行存储器。

## 上下文

STM32N6 的 FMC 控制器提供并行存储器接口，
支持多种存储器类型和时序配置。
DS14791 Section 36: FMC。

FMC 可用于连接：
- 外部 SRAM（扩展内存）
- NOR Flash（代码存储）
- NAND Flash（大容量存储）

## 决策

<!-- 实施时填写 FMC 配置方案 -->

## 设计

### 模块架构

```
FMC 控制器
├── 存储器类型
│   ├── SRAM/PSRAM
│   ├── NOR Flash
│   ├── NAND Flash
│   └── SDRAM (如有)
│
├── 时序配置
│   ├── 地址建立/保持时间
│   ├── 数据建立/保持时间
│   └── 总线周转时间
│
└── 地址空间
    ├── Bank1: NOR/SRAM (0x60000000)
    ├── Bank2: NAND (0x70000000)
    └── Bank3: NAND (0x80000000)
```

## 实施步骤

1. [ ] 实现 FMC 基本初始化
2. [ ] 实现 SRAM/PSRAM 读写
3. [ ] 实现时序配置
4. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] 外部 SRAM 读写正确
- [ ] 时序配置正确

## 理由

<!-- 实施时填写 -->

## 依赖

- **被阻塞**: ADR-005, ADR-006
- **阻塞**: —

## 文件

- `arch/arm/stm32n6/src/stm32n6_fmc.c` — [新增] FMC 驱动
- `arch/arm/stm32n6/src/stm32n6_fmc.h` — [新增] FMC 定义

## 参考

- DS14791 Section 36: FMC
- NuttX: `arch/arm/src/stm32h7/stm32_fmc.c`

## 变更历史

- 2026-07-08: Proposed
