# ADR-037: 加密加速器（SAES, HASH, RNG）

## Renode 模型说明

`tests/renode/peripherals/STM32N6_CRYP.cs` 是当前测试基础设施中的
加密加速器仿真模型（命名为 CRYP，覆盖 ADR 标题中的 SAES 部分）。
需要明确：CMSIS `stm32n647xx.h` 中既没有 `CRYP_TypeDef`/
`SAES_TypeDef`，也没有 `CRYP1_BASE`/`CRYP2_BASE`/`SAES_BASE` 等
基地址常量——只在 SYSCFG 防篡改复位掩码的注释里提到过 "SAES,
CRYP1/2, and HASH" 的名字，寄存器布局完全未公开（与之相对，同为
加密模块的 HASH 外设则有完整的 `HASH_TypeDef` 公开定义）。因此该
模型的 `CR@0x00/SR@0x04/DIN@0x08/DOUT@0x0C/KEYR0-3@0x10-0x1C/
IVR0-3@0x20-0x2C` 布局是团队参照通用 AES 协处理器惯例自定义的占位
设计，不能作为真实驱动的参考。未来实现真实 SAES 驱动时，必须以 ST
官方 HAL（`stm32n6_hal_saes.h`）或 DS14791 SAES 章节的专门寄存器
手册为准。

## 状态

Accepted

## Phase

P4

## 目标

> GitHub Issue: #42
> 验证级别: BUILD

实现 STM32N6 加密加速器驱动，
包括 SAES（AES）、HASH（SHA）、RNG（随机数生成器）。

## 上下文

STM32N647X0 有硬件加密引擎，特性：
- SAES: AES-128/192/256，ECB/CBC/CTR/GCM
- HASH: SHA-1/224/256/384/512，HMAC
- RNG: 真随机数发生器（TRNG）
- 硬件加速，比软件实现快 10-100 倍

DS14791 Section 15: SAES, Section 16: HASH, Section 17: RNG。
加密加速器用于安全通信、固件签名、密钥管理。

## 决策

三个模块全部实现：

- **RNG**: 用于生成 MQTT client ID 和网络连接随机化参数。
  注册为 `/dev/hwrng`，应用层按需 read() 获取真随机数。
- **SAES (AES)**: 提供 AES-128/192/256 硬件加速，支持 ECB/CBC/CTR/GCM 模式。
  通过 NuttX crypto 框架 `/dev/crypto` 接口使用。
- **HASH (SHA)**: 提供 SHA-1/224/256/384/512 硬件加速，支持 HMAC。
  通过 NuttX crypto 框架 `/dev/crypto` 接口使用。

## 设计

### 模块架构

```
加密加速器
├── SAES (AES 加密)
│   ├── AES-128/192/256
│   ├── 模式: ECB/CBC/CTR/GCM/CCM
│   ├── DMA 传输支持
│   └── 密钥寄存器 / OTP 密钥
│
├── HASH (哈希)
│   ├── SHA-1, SHA-224, SHA-256
│   ├── SHA-384, SHA-512
│   ├── HMAC
│   └── DMA 传输支持
│
└── RNG (随机数)
    ├── 真随机数 (TRNG)
    ├── 噪声源: 模拟振荡器
    └── 健康检测 (自检)
```

### NuttX 框架对接

```
NuttX crypto 框架:
  crypto_register("aes", &saes_ops)
  crypto_register("sha256", &hash_ops)
  /dev/hwrng (RNG 设备)

应用层 (加密):
  fd = open("/dev/crypto", O_RDWR)
  crypt_op op = { .op = COP_ENCRYPT, .cipher = "aes-cbc" };
  ioctl(fd, CIOCCRYPT, &op)

应用层 (随机数):
  fd = open("/dev/hwrng", O_RDONLY)
  read(fd, buf, 32)
  → 32 字节真随机数
```

### 数据流

```
AES 加密:
  明文 → SRAM
    → SAES DMA 读取 → AES 核心
    → 密文 → DMA → SRAM

SHA-256 计算:
  数据 → SRAM
    → HASH DMA 读取 → SHA 核心
    → 哈希值 (32B) → 寄存器

TLS 握手:
  RNG → 随机数生成
    → RSA/ECDH 密钥交换
    → AES-GCM 加密通道

## 实施步骤

- [x] 实现 RNG 驱动
- [ ] 实现 SAES (AES) 驱动
- [ ] 实现 HASH (SHA) 驱动
- [ ] 集成 NuttX crypto 框架
- [ ] 编译验证

## 验证标准

- [x] `ci-check.sh` 编译通过（BUILD）
- [ ] RNG 随机数通过健康检测
- [ ] AES 加密/解密正确
- [ ] SHA-256 哈希正确

## 完成状态

RNG 驱动已完成实现和编译验证（BUILD）。SAES 和 HASH 驱动待实现。

## 理由

加密加速器是安全通信、固件签名、密钥管理的基础设施。
RNG 实现简单（几个寄存器操作），已作为第一个模块完成。
SAES 和 HASH 驱动后续按计划逐步实现。

## 依赖

- **被阻塞**: ADR-005
- **阻塞**: （见 ROADMAP 依赖图）

## 文件

- arch/arm/stm32n6/src/stm32n6_rng.c
- stm32n6_hash.c
- stm32n6_aes.c

## 参考

- DS14791 Section 15: SAES, Section 16: HASH, Section 17: RNG
- NuttX: `drivers/crypto/`
- ST HAL: `stm32n6_hal_saes.h`, `stm32n6_hal_hash.h`, `stm32n6_hal_rng.h`
