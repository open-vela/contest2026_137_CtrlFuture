# ADR-025: HPDMA1 高性能 DMA

## 状态

Proposed

## Phase

P4

## 目标

> GitHub Issue: #30
> 验证级别: MEASURED

实现 HPDMA1（高性能 DMA）驱动，用于高带宽外设
（LTDC、DCMIPP、NPU）的大数据量传输。

## 上下文

STM32N6 有 HPDMA1（高性能 DMA），与 GPDMA 不同，
HPDMA 专为高带宽外设设计，支持更高传输速率。
DS14791 Section 12: HPDMA。

HPDMA 用于：
- LTDC 帧缓冲传输（ADR-031）
- DCMIPP 摄像头数据（ADR-033）
- NPU 数据搬运（ADR-036）

## 决策

<!-- 实施时填写 HPDMA 通道分配策略 -->

## 设计

### 模块架构

```
HPDMA1 (高性能 DMA)
├── 通道特性
│   ├── 更高总线带宽
│   ├── 支持 2D 传输 (LTDC/DCMIPP)
│   └── 链表模式 (Linked-list)
│
├── 通道分配
│   ├── CH0-3: LTDC 帧缓冲
│   ├── CH4-7: DCMIPP 摄像头
│   └── CH8-11: NPU 数据
│
└── 与 GPDMA 的区别
    ├── GPDMA: 通用外设 (SPI/I2C/UART)
    └── HPDMA: 高带宽 (Display/Camera/NPU)
```

### NuttX 框架对接

```
与 GPDMA 相同的 API 接口:
  arm_dma_allocate(ch)
  arm_dma_free(ch)
  arm_dma_start(ch, cfg)
  arm_dma_stop(ch)

2D 传输配置:
  cfg.src_width  = line_width
  cfg.src_lines  = num_lines
  cfg.src_stride = line_stride
```

## 实施步骤

1. [ ] 实现 HPDMA 基本初始化
2. [ ] 实现通道分配/释放
3. [ ] 实现 2D 传输模式
4. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] HPDMA 内存传输正确
- [ ] 2D 传输正确（LTDC 联合测试）

## 理由

<!-- 实施时填写 -->

## 依赖

- **被阻塞**: ADR-011
- **阻塞**: ADR-031, ADR-033, ADR-036

## 文件

- `arch/arm/stm32n6/src/stm32n6_hpdma.c` — [新增] HPDMA 驱动
- `arch/arm/stm32n6/src/stm32n6_hpdma.h` — [新增] HPDMA 定义

## 参考

- DS14791 Section 12: HPDMA
- NuttX: `arch/arm/src/stm32h7/stm32_dma.c` (参考模式)

## 变更历史

- 2026-07-08: Proposed
