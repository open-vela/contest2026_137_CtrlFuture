# ADR-006: GPIO 完善（165 引脚 AF 映射）

## 状态

Done

## Phase

P1

## 目标

> GitHub Issue: #11
> 验证级别: BUILD

完善 STM32N6 全部 165 个 GPIO 引脚的复用功能（AF）映射表，
为所有外设驱动提供统一的引脚配置接口。

## 上下文

ADR-002 仅实现了 PE5/PE6 两个引脚的 USART1 AF7 配置。
STM32N647X0 有 11 个 GPIO 端口（GPIOA-GPIOJ + GPIOZ），
共 165 个引脚，每个引脚最多支持 16 种复用功能（AF0-AF15）。

DS14791 Table 30 列出了完整的 AF 映射表。
后续每个外设驱动（SPI/I2C/UART/ETH/LTDC...）都需要
正确配置对应引脚的 AF。

## 决策

EdgeSight 项目仅实现当前所需引脚的 AF 映射，完整 165 引脚 AF 表延后：

- **USART1**: PE5 (AF7 TX), PE6 (AF7 RX) — 控制台（已有）
- **Ethernet RMII**: PA1/PA2/PA7, PB11/PB12/PB13, PC1/PC4/PC5 (AF11) — 网络
- **SDMMC1**: PC8/PC9/PC10/PC11/PC12 (AF12), PD2 (AF12) — SD 卡存储
- **I2C4**: PB6 (AF6 SCL), PB7 (AF6 SDA) — 摄像头控制
- **DCMIPP/CSI-2**: 专用引脚（无需 AF 配置，硬件固定）

32 位引脚编码方案沿用设计章节定义的 port/pin/mode/af/speed/pupd/otype 格式。
板级引脚统一定义在 `board_pinmap.h` 中，按外设分组。
其余未使用引脚保持默认模拟输入状态（低功耗）。

## 设计

### 模块架构

GPIO 驱动分为 3 层：

```
引脚定义层 (board_pinmap.h)
├── GPIO_USART1_TX  = GPIO_PORT_E | GPIO_PIN_5 | GPIO_AF7
├── GPIO_SPI1_SCK   = GPIO_PORT_A | GPIO_PIN_5 | GPIO_AF5
├── GPIO_ETH_TXD0   = GPIO_PORT_B | GPIO_PIN_12 | GPIO_AF11
└── ... (所有板级引脚定义)

GPIO 驱动层 (stm32n6_gpio.c)
├── stm32n6_configgpio(uint32_t pinset)  ← 32 位编码配置
├── stm32n6_gpioread(uint32_t pinset)    ← 读引脚电平
├── stm32n6_gpiowrite(uint32_t pinset)   ← 写引脚电平
└── stm32n6_gpioirq(...)                 ← EXTI 中断（ADR-010）

硬件层
├── GPIOx_MODER   — 模式 (输入/输出/AF/模拟)
├── GPIOx_OTYPER  — 输出类型 (推挽/开漏)
├── GPIOx_OSPEEDR — 输出速度
├── GPIOx_PUPDR   — 上拉/下拉
├── GPIOx_AFRL/H  — 复用功能选择 (AF0-AF15)
└── GPIOx_IDR/ODR — 输入/输出数据
```

### NuttX 框架对接

GPIO 驱动在 NuttX 中的使用方式：

```
外设驱动初始化时:
  stm32n6_configgpio(GPIO_USART1_TX)   ← 配置引脚 AF
  stm32n6_configgpio(GPIO_USART1_RX)

NuttX GPIO 框架 (可选):
  gpio_lower_half_s → 注册 /dev/gpioN
  → 支持 userspace GPIO 控制
```

### 数据流

```
32 位引脚编码:
  [31:28] port   (0=A, 1=B, ..., 10=Z)
  [27:24] pin    (0-15)
  [23:20] mode   (00=input, 01=output, 10=AF, 11=analog)
  [19:16] af     (0-15)
  [15:12] speed  (00=low, 01=medium, 10=high, 11=very_high)
  [11:8]  pupd   (00=none, 01=pull-up, 10=pull-down)
  [7:4]   otype  (0=push-pull, 1=open-drain)

stm32n6_configgpio(pinset):
  → 提取 port/pin/mode/af/speed/pupd/otype
  → GPIOx_MODER = mode
  → GPIOx_OSPEEDR = speed
  → GPIOx_PUPDR = pupd
  → GPIOx_OTYPER = otype
  → GPIOx_AFRL/AFRH = af
```

## 实施步骤

1. [ ] 定义完整 AF 映射表（DS14791 Table 30）
2. [ ] 创建 `board_pinmap.h` 集中定义板级引脚
3. [ ] 扩展 `stm32n6_configgpio()` 支持全部端口
4. [ ] 添加 `stm32n6_gpioread()` / `stm32n6_gpiowrite()`
5. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] 所有 AF 映射与 DS14791 Table 30 一致
- [ ] USART1 引脚配置行为不变（回归测试）


## Renode 仿真验证

> 验证平台: Renode (Cortex-M55 @ 64MHz HSI)
> 验证日期: 2026-07-11

- **C# 模型**: 使用 Renode 内置模型
- **Robot 测试**: `tests/renode/tests/006-gpio.robot`
- **测试用例**: Port E MODER Readback, Port E Output Write, Boot Regression
- **结果**: 全部通过 ✓
## 理由

EdgeSight 仅使用约 20 个引脚的 AF 功能，完整 165 引脚映射工作量大且无即时收益。
按需实现可加速开发节奏，后续如需扩展外设再补充对应引脚即可。
未使用引脚设为模拟输入可避免浮空输入导致的额外功耗。

## 依赖

- **被阻塞**: ADR-005
- **阻塞**: ADR-010, ADR-012, ADR-013, ADR-014, ADR-018 ~ ADR-024, ADR-029, ADR-031, ADR-033

## 文件

- `arch/arm/stm32n6/src/stm32n6_gpio.c` — [修改] 扩展全端口支持
- `arch/arm/stm32n6/src/stm32n6_gpio.h` — [修改] 完整 AF 定义
- `board/contest_board/include/board_pinmap.h` — [新增] 板级引脚定义

## 参考

- DS14791 Table 30: Alternate function mapping (165 pins × 16 AFs)
- DS14791 Section 9: GPIO
- NuttX: `arch/arm/src/stm32h7/stm32_gpio.h` (32-bit pin encoding)

## 变更历史

- 2026-07-08: Proposed
- 2026-07-11: Accepted (Renode L3 模型 + Robot 测试验证通过)
