# ADR-005: RCC 时钟树完善（HSE, PLL2-4, LSI/LSE）

## 状态

Proposed

## Phase

P1

## 目标

> GitHub Issue: #10
> 验证级别: MEASURED

完善 STM32N6 RCC 时钟树配置，支持 HSE 外部晶振、PLL2/PLL3/PLL4 倍频、
LSI/LSE 低速时钟源，为所有外设提供精确时钟。

## 上下文

ADR-002 仅配置了 HSI 64MHz 内部振荡器作为系统时钟。
STM32N6 的外设需要多种时钟频率：
- CPU 最高 800MHz（需 PLL 倍频）
- USB 需要 48MHz 精准时钟
- RTC 需要 32.768kHz LSE
- 看门狗需要 LSI ~32kHz

DS14791 Section 7: RCC 时钟树包含 4 个 PLL（PLL1-4），
每个 PLL 有独立的 VCO、分频/倍频因子。

## 决策

<!-- 实施时填写具体 PLL 配置方案 -->

## 设计

### 模块架构

RCC 时钟树分为 4 层：

```
时钟源层
├── HSI 16MHz (内部高速)     ← ADR-002 已使用
├── HSE 32MHz (外部晶振)     ← 本 ADR 新增
├── CSI 4MHz (内部低功耗)    ← 低功耗模式备用
├── LSI ~32kHz (内部低速)    ← IWDG 看门狗
└── LSE 32.768kHz (外部低速) ← RTC 实时时钟

PLL 层
├── PLL1 (系统时钟)          ← CPU/AHB/APB 总线
├── PLL2 (外设时钟)          ← SPI/I2C/USART
├── PLL3 (外设时钟)          ← USB/SAI
└── PLL4 (外设时钟)          ← LTDC/DSI

时钟输出层
├── SYSCLK → CPU (最高 800MHz)
├── AHB1-5 分频 → DMA/GPIO/Ethernet
├── APB1-4 分频 → USART/SPI/I2C/TIM
└── 专用时钟 → USB48M/SAI/ETH_REF
```

### NuttX 框架对接

RCC 驱动在 NuttX 启动链中的位置：

```
__start()
  → stm32n6_clockconfig()     ← ADR-002 最小实现
    → rcc_enablehsi()         ← 已实现
    → rcc_enablegpioe()       ← 已实现
    → rcc_enableusart1()      ← 已实现
    → rcc_enablepll()         ← 本 ADR 扩展
    → rcc_configsysclk()     ← 本 ADR 扩展

NuttX 外设驱动通过以下 API 获取时钟频率：
  → up_get_sysclk()          ← SYSCLK 频率
  → up_get_periphclk(bus)    ← 外设总线时钟
```

### 数据流

```
HSE 32MHz (外部晶振)
  │
  └─→ PLL1 (VCO: 100-800MHz)
       ├─ DIVM1 分频 → PREDIV
       ├─ DIVN1 倍频 → VCO
       └─ DIVP1/DIVQ1/DIVR1 分频
            │
            ├─ SYSCLK (最高 800MHz)
            │    ├─ AHB prescaler → HCLK (CPU)
            │    └─ APB1-4 prescaler → 外设时钟
            │
            ├─ PLL2 → SPI/I2C/USART 时钟
            ├─ PLL3 → USB 48MHz
            └─ PLL4 → LTDC 像素时钟
```

## 实施步骤

1. [ ] 定义 RCC 寄存器结构（`stm32n6_rcc.h`）
2. [ ] 实现 HSE 使能和就绪检测
3. [ ] 实现 PLL1 配置（VCO/分频/倍频）
4. [ ] 实现系统时钟切换（HSI → PLL1）
5. [ ] 实现 PLL2/PLL3/PLL4 配置
6. [ ] 实现 LSI/LSE 使能
7. [ ] 提供 `up_get_sysclk()` / `up_get_periphclk()` API
8. [ ] 编译验证 + 真机测量时钟频率

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] PLL1 输出目标频率（示波器/MCO 引脚测量）
- [ ] USART1 波特率在 PLL 时钟下仍然正确
- [ ] 各外设时钟频率与 datasheet 一致

## 理由

<!-- 实施时填写 PLL 选型和频率规划的理由 -->

## 依赖

- **被阻塞**: ADR-004
- **阻塞**: ADR-006 ~ ADR-038（几乎所有后续模块依赖时钟树）

## 文件

- `arch/arm/stm32n6/src/stm32n6_rcc.c` — [修改] 扩展时钟配置
- `arch/arm/stm32n6/src/hardware/stm32_rcc.h` — [新增] RCC 寄存器定义
- `arch/arm/stm32n6/src/stm32n6_rcc.h` — [修改] 添加时钟查询 API

## 参考

- DS14791 Section 7: Clock tree (RCC)
- DS14791 Section 7.4: PLL configuration
- NuttX: `arch/arm/src/stm32h7/stm32h7x3_rcc.c`
- NuttX: `arch/arm/src/stm32h7/stm32_clockconfig.c`

## 变更历史

- 2026-07-08: Proposed
