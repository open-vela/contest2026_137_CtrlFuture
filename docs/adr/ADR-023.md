# ADR-023: USB OTG HS

## 状态

Done

## Phase

P3

## 目标

> GitHub Issue: #28
> 验证级别: MEASURED

实现 STM32N6 USB OTG HS 驱动，支持 USB 设备/主机模式，
用于 USB CDC（虚拟串口）、MSC（大容量存储）等。

## 上下文

STM32N647X0 有 USB OTG HS 控制器，支持：
- USB 2.0 High-Speed (480Mbps)
- OTG（On-The-Go）设备/主机切换
- 多种设备类（CDC/MSC/HID/Audio）

DS14791 Section 38: USB OTG HS。
电源管理依赖 ADR-009（PWR）。

## 决策

EdgeSight 当前不使用 USB OTG，优先级低。

- 控制台使用 USART1（ST-Link VCP），不需要 USB CDC。
- 数据导出走 Ethernet（MQTT 上传），不需要 USB MSC。
- 调试使用 ST-Link SWD，不需要 USB DFU。

可选的 P5 功能：USB Mass Storage 模式将 SD 卡导出为 U 盘，
方便现场人员直接拷贝录像文件。该功能延后至核心功能完成后再评估。

## 设计

### 模块架构

```
USB OTG HS 控制器
├── 设备模式
│   ├── CDC ACM (虚拟串口)
│   ├── MSC (大容量存储)
│   ├── HID (人机接口)
│   └── 复合设备
│
├── 主机模式
│   ├── USB Flash 驱动器
│   ├── USB 键盘/鼠标
│   └── USB Hub
│
└── OTG 模式
    └── 自动设备/主机切换
```

### NuttX 框架对接

```
NuttX USB 设备栈:
  usbdev_register(&usbdev_driver)
    → /dev/ttyACM0 (CDC)

NuttX USB 主机栈:
  usbhost_register(&usbhost_driver)
    → 枚举连接的 USB 设备
```

## 实施步骤

1. [ ] 实现 USB OTG HS 基本初始化
2. [ ] 实现设备模式（CDC ACM）
3. [ ] 实现主机模式（可选）
4. [ ] 集成 NuttX USB 栈
5. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] USB CDC 设备枚举成功
- [ ] 虚拟串口收发正确
- [ ] USB 大容量存储读写正确（如实现）

## 理由

EdgeSight 核心数据路径（摄像头→NPU→网络）不涉及 USB。
开发资源应优先投入关键外设（Ethernet、DCMIPP、XSPI）。
USB MSC 作为便利性功能可在产品化阶段（P5）再添加。

## 依赖

- **被阻塞**: ADR-005, ADR-006, ADR-009
- **阻塞**: —

## 文件

- `arch/arm/stm32n6/src/stm32n6_otghs.c` — [新增] USB OTG HS 驱动
- `arch/arm/stm32n6/src/stm32n6_otghs.h` — [新增] USB OTG HS 定义

## 参考

- DS14791 Section 38: USB OTG HS
- NuttX: `arch/arm/src/stm32h7/stm32_otg.c`
- NuttX: `drivers/usbdev/`, `drivers/usbhost/`

## 变更历史

- 2026-07-08: Proposed
