# ADR-015: 看门狗（IWDG/WWDG）

## 状态

Proposed

## Phase

P1

## 目标

> GitHub Issue: #20
> 验证级别: MEASURED

实现 STM32N6 看门狗驱动，支持独立看门狗（IWDG）
和窗口看门狗（WWDG），防止软件死锁导致系统挂起。

## 上下文

STM32N6 有 2 种看门狗：
- IWDG：独立看门狗，使用 LSI 时钟，不受主时钟影响
- WWDG：窗口看门狗，使用 APB 时钟，有精确喂狗窗口

DS14791 Section 34: IWDG/WWDG。
看门狗是嵌入式系统可靠性保障的关键组件。

## 决策

<!-- 实施时填写看门狗策略 -->

## 设计

### 模块架构

```
IWDG (独立看门狗)
├── 时钟源: LSI ~32kHz
├── 预分频器: 4/8/16/32/64/128/256
├── 重载值: 12-bit (0-4095)
├── 超时范围: ~0.1ms ~ 32s
└── 特性: 独立于主时钟, 一旦使能无法禁用

WWDG (窗口看门狗)
├── 时钟源: APB (经过分频)
├── 窗口值: 7-bit
├── 计数器: 7-bit
└── 特性: 有精确喂狗窗口, 可产生早期中断
```

### NuttX 框架对接

```
NuttX watchdog 框架:
  watchdog_register("/dev/watchdog0", &iwdg_dev)
    → open("/dev/watchdog0")
    → ioctl(WDIOC_SETTIMEOUT, &timeout)
    → ioctl(WDIOC_START)
    → 定期 ioctl(WDIOC_KEEPALIVE) ← 喂狗

自动喂狗 (board_bringup.c):
  board_wdog_init() → 启动看门狗
  work_queue() → 定期喂狗
```

## 实施步骤

1. [ ] 实现 IWDG 驱动
2. [ ] 实现 WWDG 驱动
3. [ ] 集成 NuttX watchdog 框架
4. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] IWDG 超时后系统复位
- [ ] WWDG 窗口外喂狗触发复位
- [ ] 正常喂狗系统不复位

## 理由

<!-- 实施时填写 -->

## 依赖

- **被阻塞**: ADR-005
- **阻塞**: —

## 文件

- `arch/arm/stm32n6/src/stm32n6_iwdg.c` — [新增] IWDG 驱动
- `arch/arm/stm32n6/src/stm32n6_wwdg.c` — [新增] WWDG 驱动

## 参考

- DS14791 Section 34: IWDG, WWDG
- NuttX: `arch/arm/src/stm32h7/stm32_iwdg.c`
- NuttX: `drivers/timers/watchdog.c`

## 变更历史

- 2026-07-08: Proposed
