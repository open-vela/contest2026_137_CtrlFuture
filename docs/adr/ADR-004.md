# ADR-004: 真机串口验证（DEV boot SRAM 加载）

## 状态

<!-- 合法值: Proposed | Accepted | Deprecated -->

Accepted（真机 MEASURED —— USART1 NSH 提示符 + builtin 执行在
STM32N647 上反复验证，见「实现说明」）

## Phase

P0

## 目标

> GitHub Issue: #9
> 验证级别: MEASURED

在 STM32N6570-DK 开发板上，通过 DEV boot 模式从 SRAM 加载 NuttX
固件，USART1 (PE5/PE6) 输出 NSH shell 提示符，验证完整启动链
在真实硬件上工作。

## 上下文

ADR-001/002/003 已完成 CI 编译验证（BUILD 级别）。
P0 的最终目标是真机串口输出 `nsh>`，这需要：

1. **DEV boot 模式**：STM32N6 无内部 flash，代码必须通过
   ST-Link 加载到 SRAM（0x34000400）。ROM bootloader 在
   SRAM 最前 1KB 放置 boot header。
2. **ST-Link VCP**：STM32N6570-DK 板载 ST-Link 提供虚拟串口，
   连接到 USART1 PE5(TX)/PE6(RX)。
3. **工具链**：需要 STM32CubeProgrammer 或 OpenOCD 进行
   SRAM 加载和调试。

## 决策

使用 STM32CubeProgrammer CLI 通过 ST-Link 将 nuttx.bin 加载到 SRAM 0x34000400。
开发板设置 DEV boot 模式（BOOT0=1），确保 CPU 从 SRAM 启动。
串口终端配置：115200 波特率，8N1，无流控。
验证成功标准：串口终端显示 `nsh>` 提示符。

加载命令示例：
```
STM32_Programmer_CLI -c port=SWD mode=HOTPLUG \
  -w nuttx.bin 0x34000400 \
  -s 0x34000400
```

硬件连接：ST-Link VCP 自动映射到 USART1 PE5(TX)/PE6(RX)，
主机端通过 /dev/ttyACM0（Linux）或 COMx（Windows）连接。

## 设计

### 模块架构

本 ADR 是验证里程碑，不新增代码文件。
验证的是 ADR-002 + ADR-003 的芯片驱动在真实硬件上的行为。

### NuttX 框架对接

验证的完整启动链：

```
ST-Link 加载 nuttx.bin → SRAM @ 0x34000400
  → CPU 从 _stext 开始执行
    → __start() [stm32n6_start.c]
      → BSS 清零 + DATA 拷贝
      → stm32n6_clockconfig()  ← HSI 64MHz
      → stm32n6_lowsetup()     ← USART1 PE5/PE6 AF7
      → nx_start()
        → up_irqinitialize()   ← NVIC 初始化
        → up_allocate_heap()   ← 堆注册
        → arm_serialinit()     ← 注册 /dev/console
        → up_timer_initialize()← SysTick @ 100Hz
        → nsh_main()           ← NSH shell 启动
          → 输出 "nsh>" 提示符
```

### 数据流

```
NSH 输入:
  用户键盘 → ST-Link VCP → USART1 RX (PE6)
    → USART1 RXNE 中断 → stm32n6_interrupt()
      → uart_recvchars() → NSH readline

NSH 输出:
  NSH printf → uart_send() → USART1 TX (PE5)
    → USART1 TXE 中断 → stm32n6_interrupt()
      → uart_xmitchars() → ST-Link VCP → 串口终端
```

## 实施步骤

1. [x] 准备硬件环境（远程 STM32N647 + 跳板机两跳 SSH + SWD/GDB）
2. [x] 配置 SWD/GDB SRAM 加载（reset/load/continue，见 hw-verify.sh）
3. [x] 编译 nsh / nsh-test 固件并加载到 SRAM
4. [x] 验证串口终端输出 NSH 提示符
5. [x] 验证 `help`、`hello`、`uname`、`ps` 命令正常工作
6. [~] 逐项复核 [UPSTREAM_PORTING_GAPS.md](../../arch/arm/stm32n6/src/UPSTREAM_PORTING_GAPS.md)
   中列出的、因缺少真机验证条件而暂未移植的官方修复
   （MSPLIM/PSPLIM 复位状态、ES0620/BSEC erratum、
   RTC backup domain 写保护等）—— 随后续 ADR 真机周期逐条复核

## 实现说明

### 真机验证方式（MEASURED，2026-07-30）

原方案假设本地插板 + STM32CubeProgrammer；实际验证走远程真机：
两跳 SSH（跳板机中转 ELF → 目标机），GDB `reset/load/continue` 把
固件加载到 SRAM 0x34000400，再 `stty` + `cat` 采集 USART1 串口输出。
自动化封装在 `scripts/hw-verify.sh`（真机门禁，本地脚本不入库）与
`scripts/hw-verify-drivertest.sh`（builtin 回归）。

完整启动链已在 STM32N647 上反复验证：SWD 加载后 CPU 从 `_stext` 运行，
经 `__start()` → 时钟/串口低级初始化 → `nx_start()` → NSH 启动，
USART1 打印 NSH 提示符，`help`/`hello`/`uname`/`ps` 与串口输入回显
均正常，无 `irq_unexpected_isr`。此后每个影响真机行为的 ADR
（027/028/029/030/032/037…）都依赖同一条串口链采集验证结果，
构成对 ADR-004 启动链的持续回归证据。

### 代码模式

无新代码 —— 本 ADR 验证 ADR-002/003 芯片驱动在真机上的启动行为。

### 踩坑与解决

- 本地无插板，改用远程真机两跳 SSH + GDB 加载，替代
  STM32CubeProgrammer 本地方案。
- 串口捕获需先 `stty` 设定 115200/8N1 再 `cat`，否则波特率
  不匹配导致乱码。

## 验证标准

- [x] `ci-check.sh` 编译通过（BUILD）
- [x] nsh 固件加载到 SRAM 后 CPU 正常运行
- [x] USART1 输出 NSH 提示符（MEASURED）
- [x] `help` 命令返回 NSH 命令列表
- [x] `hello` 命令输出 "Hello, World!"
- [x] `ps` 命令显示任务列表
- [x] 串口输入回显正常（键盘输入可见）

## 理由

选择 STM32CubeProgrammer 而非 OpenOCD，因为其对 STM32N6 的支持更成熟，
且 ST-Link 固件更新和 SRAM 加载流程均由 ST 官方工具链保证兼容性。
DEV boot 模式是 STM32N6 无内部 Flash 芯片的标准开发调试方式。
115200 波特率在 HSI 64MHz 下误差极小，兼容性最佳。

## 依赖

- **被阻塞**: ADR-002（最小启动驱动）, ADR-003（SysTick 调度器时钟）
- **阻塞**: ADR-005（RCC 时钟树完善，需要真机验证基础）

## 文件

- 无新代码文件 — 验证里程碑

## 参考

- DS14791 Section 7: Memory map (SRAM @ 0x34000000)
- DS14791 Section 6: Boot modes (DEV boot)
- STM32N6570-DK User Manual: ST-Link VCP 连接
- NuttX: `boards/arm/mps/mps3-an547/` — QEMU 参考板

## 变更历史

- 2026-07-08: Proposed
- 2026-07-30: Accepted —— 真机 MEASURED。远程 STM32N647 两跳 SSH + GDB
  加载 SRAM，USART1 打印 NSH 提示符、builtin 执行正常，启动链验证通过。
  后续每个真机 ADR 周期依赖同一串口链，构成持续回归证据。
