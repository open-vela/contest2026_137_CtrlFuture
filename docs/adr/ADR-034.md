# ADR-034: CSI-2 MIPI 接口

## 状态

Proposed

## Phase

P4

## 目标

> GitHub Issue: #39
> 验证级别: MEASURED

实现 STM32N6 CSI-2（MIPI Camera Serial Interface 2）驱动，
支持 MIPI CSI-2 协议的物理层和协议层。

## 上下文

STM32N647X0 内置 CSI-2 接口，特性：
- MIPI CSI-2 v2.1 协议
- 支持 2-lane / 4-lane 配置
- 数据速率最高 2.5Gbps/lane
- 支持 RAW8/10/12、YUV422、RGB888
- D-PHY 物理层

DS14791 Section 40: CSI-2。
CSI-2 是现代摄像头模组的标准接口。

## 决策

<!-- 实施时填写 CSI-2 lane 配置和数据速率 -->

## 设计

### 模块架构

```
CSI-2 接口
├── D-PHY 物理层
│   ├── 差分时钟对 (CLK+/CLK-)
│   ├── 数据差分对 (D0+/D0- ... D3+/D3-)
│   ├── LP 模式 (低功耗, 命令)
│   └── HS 模式 (高速, 数据)
│
├── 协议层
│   ├── 包格式 (Start/End/DataType)
│   ├── 虚拟通道 (VC0-VC3)
│   ├── CRC 校验
│   └── ECC 校验
│
└── 数据类型
    ├── RAW6/7/8/10/12/14
    ├── YUV422 (8/10-bit)
    ├── RGB565/888
    └── User-Defined
```

### NuttX 框架对接

```
CSI-2 作为 DCMIPP 输入:
  csi_init(lanes, data_rate)
  → D-PHY 初始化
  → 协议层配置
  → DCMIPP (ADR-033) 接收数据

摄像头模组集成:
  OV5640 → CSI-2 → DCMIPP → 帧缓冲
  IMX335 → CSI-2 → DCMIPP → 帧缓冲
```

### 数据流

```
数据接收:
  CMOS 传感器 → D-PHY (HS 模式)
    → 协议层解析 (包头/CRC)
    → 虚拟通道分离
    → DCMIPP 像素管线
    → ISP 处理 → 帧缓冲

## 实施步骤

1. [ ] 实现 D-PHY 初始化
2. [ ] 实现 CSI-2 协议层配置
3. [ ] 实现虚拟通道和数据类型
4. [ ] 集成 DCMIPP 数据接收
5. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] CSI-2 链路建立
- [ ] 摄像头数据正确接收

## 理由

（开发时填写）

## 依赖

- **被阻塞**: ADR-033
- **阻塞**: （见 ROADMAP 依赖图）

## 文件

- arch/arm/stm32n6/src/stm32n6_csi.c/.h

## 参考

- DS14791 Section 40: CSI-2
- MIPI Alliance: CSI-2 v2.1 Specification
- ST HAL: `stm32n6_hal_csi.h`
