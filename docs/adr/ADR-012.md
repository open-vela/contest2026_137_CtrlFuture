# ADR-012: 额外 USART/UART 驱动

## 状态

Proposed

## Phase

P1

## 目标

> GitHub Issue: #17
> 验证级别: MEASURED

扩展 NuttX serial 驱动支持 STM32N6 的全部 USART/UART 实例，
支持多串口同时工作。

## 上下文

ADR-002 仅实现了 USART1（PE5/PE6）作为控制台。
STM32N647X0 有多个 USART/UART 实例：
- USART1-3（同步/异步）
- UART4-8（异步）

DS14791 Section 28: USART。
多串口用于调试日志、GPS 模块、蓝牙通信等场景。

## 决策

<!-- 实施时填写多串口管理方案 -->

## 设计

### 模块架构

```
扩展 serial 驱动 (stm32n6_serial.c)
├── g_uart1_port — USART1 (已有, 控制台)
├── g_uart2_port — USART2 (新增)
├── g_uart3_port — USART3 (新增)
├── g_uart4_port — UART4  (新增)
└── ...

每个端口包含:
├── uart_ops_s 回调结构体
├── TX/RX 环形缓冲区 (64B)
├── IRQ 编号
└── 时钟/引脚配置
```

### NuttX 框架对接

```
arm_serialinit():
  uart_register("/dev/console", &g_uart1_port)  ← 已有
  uart_register("/dev/ttyS1", &g_uart2_port)    ← 新增
  uart_register("/dev/ttyS2", &g_uart3_port)    ← 新增
  ...

应用层使用:
  fd = open("/dev/ttyS1", O_RDWR)
  read(fd, buf, len)   ← UART2 接收
  write(fd, buf, len)  ← UART2 发送
```

## 实施步骤

1. [ ] 扩展 `stm32n6_serial.c` 支持多端口
2. [ ] 每个 UART 独立使能时钟（ADR-005）
3. [ ] 配置各 UART 引脚 AF
4. [ ] 注册为 `/dev/ttySN` 设备
5. [ ] 编译验证

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] USART1 控制台行为不变（回归测试）
- [ ] 多个 UART 可同时收发数据

## 理由

<!-- 实施时填写 -->

## 依赖

- **被阻塞**: ADR-005, ADR-006
- **阻塞**: —

## 文件

- `arch/arm/stm32n6/src/stm32n6_serial.c` — [修改] 扩展多端口

## 参考

- DS14791 Section 28: USART
- NuttX: `arch/arm/src/stm32h7/stm32_serial.c` (多端口注册)

## 变更历史

- 2026-07-08: Proposed
