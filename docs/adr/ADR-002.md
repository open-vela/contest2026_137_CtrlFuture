# ADR-002: 最小启动（RCC/GPIO/USART1/IRQ/heap/start）

## 状态

Accepted

## Phase

P0

## 目标

> GitHub Issue: #7
> 验证级别: BUILD

STM32N6 芯片驱动通过编译，包含最小启动所需的全部模块：
时钟、GPIO、USART1 串口、中断控制器、堆分配、启动代码。

## 上下文

STM32N647X0 是 Cortex-M55（ARMv8.1-M）芯片，NuttX 上游尚无
STM32N6 系列支持。需要从零实现最小芯片驱动集，使固件能从
SRAM DEV boot 模式启动并输出串口日志。

参考 STM32H7 和 MPS3-AN547 的已有实现，结合 DS14791 datasheet。

## 决策

最小驱动集包含 8 个源文件：

| 文件 | 功能 |
|------|------|
| `stm32n6_start.c` | 复位入口，BSS/DATA 初始化，调用 `nx_start()` |
| `stm32n6_rcc.c` | HSI 64MHz 时钟，GPIOE + USART1 时钟使能 |
| `stm32n6_gpio.c` | PE5/PE6 配置为 USART1 AF7 |
| `stm32n6_lowputc.c` | USART1 低级初始化和轮询发送 |
| `stm32n6_serial.c` | NuttX serial 框架驱动，中断收发 |
| `stm32n6_irq.c` | NVIC 初始化、IRQ enable/disable/prioritize |
| `stm32n6_allocateheap.c` | SRAM 堆区域注册 |
| `stm32n6_gpio.h` | GPIO 引脚定义和 AF 映射 |

PLL 配置暂不实现，CPU 运行在 HSI 64MHz。

## 实施步骤

1. [x] 创建 `arch/arm/stm32n6/` 目录结构（src + include）
2. [x] 实现 `stm32n6_start.c` 复位入口
3. [x] 实现 `stm32n6_rcc.c` 最小时钟配置
4. [x] 实现 `stm32n6_gpio.c` PE5/PE6 USART1 引脚
5. [x] 实现 `stm32n6_lowputc.c` USART1 低级输出
6. [x] 实现 `stm32n6_serial.c` NuttX serial 驱动 + `up_putc()`
7. [x] 实现 `stm32n6_irq.c` NVIC 驱动
8. [x] 实现 `stm32n6_allocateheap.c` 堆注册
9. [x] 配置 `Make.defs` / `CMakeLists.txt` 构建文件
10. [x] 编译验证通过

## 验证标准

- [x] `ci-check.sh` 编译通过（BUILD）
- [x] nsh 目标零 error 零 warning 链接成功
- [x] 二进制体积 < 1MB

## 理由

选择 HSI 64MHz 而非立即配置 PLL 的原因：
- DEV boot 模式下 ROM bootloader 已将 HSI 配置好
- 减少初始化复杂度，PLL 可在后续 ADR-005 中实现
- 64MHz 足以驱动 USART1 串口调试

选择 PE5/PE6 作为 USART1 引脚：
- STM32N6570-DK 开发板 ST-Link VCP 连接到 PE5(TX)/PE6(RX)
- AF7 功能，参照 DS14791 Table 30

## 依赖

- **被阻塞**: —
- **阻塞**: ADR-003, ADR-004, ADR-005 ~ ADR-017

## 文件

- `arch/arm/stm32n6/src/stm32n6_start.c`
- `arch/arm/stm32n6/src/stm32n6_rcc.c`
- `arch/arm/stm32n6/src/stm32n6_rcc.h`
- `arch/arm/stm32n6/src/stm32n6_gpio.c`
- `arch/arm/stm32n6/src/stm32n6_gpio.h`
- `arch/arm/stm32n6/src/stm32n6_lowputc.c`
- `arch/arm/stm32n6/src/stm32n6_lowputc.h`
- `arch/arm/stm32n6/src/stm32n6_serial.c`
- `arch/arm/stm32n6/src/stm32n6_irq.c`
- `arch/arm/stm32n6/src/stm32n6_allocateheap.c`
- `arch/arm/stm32n6/src/hardware/stm32_memorymap.h`
- `arch/arm/stm32n6/include/chip.h`
- `arch/arm/stm32n6/include/irq.h`
- `arch/arm/stm32n6/src/chip.h`
- `arch/arm/stm32n6/src/Make.defs`
- `arch/arm/stm32n6/src/CMakeLists.txt`
- `arch/arm/stm32n6/src/Kconfig`

## 参考

- DS14791 Rev 9: Section 7 (Memory map), Section 28 (USART)
- DS14791 Table 30: Alternate function mapping
- NuttX: `arch/arm/src/stm32h7/stm32_start.c`
- NuttX: `arch/arm/src/stm32h7/stm32_serial.c`
- NuttX: `arch/arm/src/mps/mps_timerisr.c`
