# ADR-019: XSPI Flash 启动

## 状态

Proposed

## Phase

P2

## 目标

> GitHub Issue: #24
> 验证级别: MEASURED

实现从外部 XSPI Flash 启动 NuttX 固件，
替代 SRAM DEV boot 模式，支持固件持久化存储。

## 上下文

当前 P0 使用 SRAM DEV boot 模式（ADR-004），
固件需要每次通过 ST-Link 加载。
XSPI Flash 启动可以：
- 固件持久化，断电不丢失
- 支持 OTA 升级
- 释放 SRAM 空间给应用

DS14791 Section 6: Boot modes (XSPI boot)。

## 决策

<!-- 实施时填写启动方案 -->

## 设计

### 模块架构

```
启动模式切换
├── DEV boot (当前, P0)
│   └── ST-Link → SRAM @ 0x34000400
│
└── XSPI boot (本 ADR)
    ├── ROM bootloader 从 XSPI Flash 加载
    ├── Flash 地址映射到内存空间
    └── 支持 XIP (Execute In Place)

链接脚本变更:
  .text : { *(.text*) } > FLASH (XSPI)
  .data : { *(.data*) } > SRAM AT> FLASH
  .bss  : { *(.bss*)  } > SRAM
```

### NuttX 框架对接

```
启动链变更:
  ROM bootloader
    → 从 XSPI Flash 加载固件到 SRAM (或 XIP)
    → 跳转到 _stext
    → __start() (与 ADR-002 相同)
    → nx_start()

OTA 升级:
  应用 → MTD write() → Flash 分区 A/B
    → 设置 boot flag → 复位 → 新固件启动
```

## 实施步骤

1. [ ] 配置 XSPI Flash 启动模式
2. [ ] 更新链接脚本（Flash → SRAM 布局）
3. [ ] 实现 Flash 烧写工具
4. [ ] 验证冷启动从 Flash 加载
5. [ ] 实现双分区 OTA 升级（可选）

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] 固件烧写到 Flash 后可冷启动
- [ ] NSH shell 正常工作
- [ ] 断电重启后固件不丢失

## 理由

<!-- 实施时填写 -->

## 依赖

- **被阻塞**: ADR-018
- **阻塞**: ADR-038

## 文件

- `board/contest_board/scripts/flash.ld` — [修改] Flash 启动布局
- XSPI Flash 烧写配置

## 参考

- DS14791 Section 6: Boot modes
- DS14791 Section 32: XSPI
- NuttX: `boards/arm/stm32h7/stm32h743zi-nucleo/` (Flash 启动参考)

## 变更历史

- 2026-07-08: Proposed
