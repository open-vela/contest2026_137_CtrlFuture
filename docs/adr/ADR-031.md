# ADR-031: LTDC LCD 控制器

## 状态

Proposed

## Phase

P4

## 目标

> GitHub Issue: #36
> 验证级别: MEASURED

实现 STM32N6 LTDC（LCD-TFT Display Controller）驱动，
支持 RGB 接口 LCD 显示屏的帧缓冲传输。

## 上下文

STM32N647X0 有 LTDC 控制器，特性：
- 支持 RGB888/RGB565 像素格式
- 2 层叠加（Layer 1 + Layer 2）
- Alpha 混合（逐像素或全局）
- 最高 1024x768 分辨率（受限于像素时钟）
- 依赖 HPDMA（ADR-025）进行帧缓冲传输

DS14791 Section 35: LCD-TFT display controller (LTDC)。
LTDC 用于 HMI 显示、仪表盘、监控界面。

## 决策

<!-- 实施时填写 LCD 面板参数和像素格式 -->

## 设计

### 模块架构

```
LTDC 控制器
├── 时序生成
│   ├── 水平同步 (HSYNC)
│   ├── 垂直同步 (VSYNC)
│   ├── 数据使能 (DE)
│   └── 像素时钟 (PCLK)
│
├── 图层 (Layer 1/2)
│   ├── 帧缓冲基地址
│   ├── 像素格式 (RGB888/RGB565/ARGB8888)
│   ├── 窗口位置和大小
│   ├── Alpha 混合 (全局/逐像素)
│   └── 色键 (Color Keying)
│
└── 输出
    ├── RGB 并行接口 (24-bit)
    └── 像素时钟 (最高 ~50MHz)
```

### NuttX 框架对接

```
NuttX framebuffer 框架:
  fb_register("/dev/fb0", &ltdc_fb)

应用层:
  fd = open("/dev/fb0", O_RDWR)
  ioctl(fd, FBIOGET_VIDEOINFO, &vinfo)
  → vinfo.xres, vinfo.yres, vinfo.bpp
  buf = mmap(NULL, size, PROT_WRITE, MAP_SHARED, fd, 0)
  → 直接写入帧缓冲
  ioctl(fd, FBIOPAN_DISPLAY, &vinfo)
  → 切换显示

图形栈:
  LittlevGL / LVGL → /dev/fb0 → LTDC → LCD
```

### 数据流

```
显示更新:
  应用 → mmap 写入帧缓冲
    → LTDC 从 SRAM 读取 (HPDMA)
    → 像素格式转换
    → 时序生成 → RGB 输出 → LCD 面板

双缓冲:
  buf0 → LTDC 显示中
  buf1 → 应用绘制中
  FBIOPAN_DISPLAY → 交换 → 无撕裂

## 实施步骤

1. [ ] 实现 LTDC 时序和层配置
2. [ ] 实现帧缓冲 mmap
3. [ ] 实现 Alpha 混合和色键
4. [ ] 集成 NuttX framebuffer 框架
5. [ ] 编译验证 + LCD 真机测试

## 验证标准

- [ ] `ci-check.sh` 编译通过（BUILD）
- [ ] LCD 显示正确（纯色/渐变/文字）
- [ ] 双缓冲切换无撕裂

## 理由

（开发时填写）

## 依赖

- **被阻塞**: ADR-005, ADR-006, ADR-007, ADR-025
- **阻塞**: （见 ROADMAP 依赖图）

## 文件

- arch/arm/stm32n6/src/stm32n6_ltdc.c/.h

## 参考

- DS14791 Section 35: LCD-TFT display controller (LTDC)
- NuttX: `drivers/video/fb.c`
- NuttX: `arch/arm/src/stm32h7/stm32_ltdc.c` (如有)
